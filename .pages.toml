# Cloudflare Pages Configuration
# For Accord and Harmony Foundation Website

# This is a static HTML/CSS/JavaScript site
# No build process required

# Note: File exclusions are handled by .cfignore file
# PHP files (api/, php/) and workers/ are deployed separately
# and are excluded from the static site deployment
